/**Editor If
Neste desafio, você usará uma if statement e o operador de igualdade === para executar um bloco de código apenas se o teste for verdadeiro. Se o teste dentro dos parênteses () for verdadeiro, o código dentro das {} do bloco será executado. Por exemplo:

if (x === 2) {
print(x);
}
If x é igual a 2, então print(x) será executado.

INSTRUÇÕES
Dentro das {} do bloco de código da if statement:

Adicione drawBox(blue) */

var color = pickRandom([
  blue,
  green,
  
]);
print(color);
if (color === blue) {
  drawBox(blue);
}
/**Quadriculado
Neste desafio, você usará indexação da array para acessar itens dentro de uma array. Indexação da array começa em 0. Por exemplo, myArray[0] vincula-se ao 1º item na array denominada myArray. Enquanto myArray[1] se vincula ao 2º item.

INSTRUÇÕES
Use indexação da array para criar um quadriculado.

Atualize os números em drawBox(colors[]) de modo que 0 seja preto e 1 seja branco */

var colors = [
    'black',
    'white',
    ___
  ];
  drawBox(colors[0]);
  drawBox(colors[1]);
  drawBox(colors[1]);
  newLine();
  drawBox(colors[0]);
  drawBox(colors[1]);
  drawBox(colors[1]);
  newLine();
  drawBox(colors[0]);
  drawBox(colors[1]);
  drawBox(colors[1]);
  ___